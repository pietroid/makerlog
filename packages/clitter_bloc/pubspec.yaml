name: clitter_bloc
description: Bloc integration for the Clitter terminal-UI framework — BlocProvider and BlocBuilder backed by clitter's BuildContext.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  clitter:
    path: ../clitter
  bloc: ^9.0.0
